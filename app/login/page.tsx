import { LoginClient } from "@/components/auth/login-client";

// This is a simple Server Component.
// Its only job is to render the client-side component for the login form.
export default function LoginPage() {
  return <LoginClient />;
}
